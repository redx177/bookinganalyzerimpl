<!DOCTYPE html>
<html lang="en">
{{ include('head.twig') }}

<body>
<div class="navigation">{{ include('navigation.twig') }}</div>
<div class="content">{% block content %}{% endblock %}</div>
{{ include('footer.twig') }}
</body>
</html>