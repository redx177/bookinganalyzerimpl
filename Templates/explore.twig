{% extends "base.twig" %}

{% block content %}
    <div class="container">
        <h1>Explore data set</h1>
        <p>On this page the data set can explored. If required, the data set can be restricted by filters</p>
    </div>

    {{ include('filters.twig') }}

    <div class="container">
        {{ include('bookingsListing.twig') }}
        {{ include('pagination.twig') }}

    </div>
{% endblock %}
